// #include "../include/philo.h"

#include <stdio.h>

int main ()
{
	// pthread_
	int *p;
	char *str = "Hello Nicole !";

	p[0] = 9;
	printf("p == %d\n", *p);
	printf("str first value is == %c\n", *str++);
	printf("str first value is == %c\n", *str);
	return (0);
}